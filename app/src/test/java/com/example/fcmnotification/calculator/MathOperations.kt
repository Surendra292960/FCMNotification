package com.example.fcmnotification.calculator

interface MathOperations {
    fun add(m: Int, n: Int): Int
    fun subtract(n: Int, m: Int): Int
    fun multiply(c: Int, a: Int): Int
    fun divide(l: Int, d: Int): Int
}